# PackTools 管理器

管理所有已安装的 PackTools 工具，提供统一入口界面。

## 安装

```
https://github.com/KevienHou/Packtools.git?path=/Packages/com.packtools.tools.manager
```

## 使用

安装后通过 Unity 菜单 `Tools > PackTools 管理器` 打开。

窗口自动扫描项目中已安装的 `com.packtools.tools.*` 包，列出名称、版本、描述，点击"打开"跳转到对应工具。
